server: 
  port: 8110

info:
  name: AdminService

spring:
  application:
    name: AdminService
  output:
    ansi:
      enabled: ALWAYS
  boot:
    admin:
      context-path: /admin  # <1>

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/

logging:
  file: logfile/admin.log

