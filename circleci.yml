machine:
  java:
    version: openjdk8

checkout:
  post:
    - chmod +x ./gradlew
    - sudo psql -p 5432 -F 'sql\dbinit.sql'

dependencies:
  override:
    - ./gradlew -v
    - ./gradlew testClasses
